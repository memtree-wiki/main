---
description: Pick and implement the next item from todo.yml
allowed-tools: [Read, Edit, Write, Bash, Agent]
---

# Todo

1. Read `todo.yml`.
2. Pick the single most actionable top-level item (or sub-item if the parent is a group).
3. Implement it fully — explore the codebase as needed, make all required changes.
4. Move **only that item** to the `Done:` section at the bottom of `todo.yml` (create it if it doesn't exist). Leave everything else untouched.
5. Report what you did in 2–3 sentences.
